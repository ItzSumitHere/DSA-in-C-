{
    "label": "Build and Run",
    "type": "shell",
    "command": "g++",
    "args": [
        "${file}",
        "-o",
        "${fileDirname}/${fileBasenameNoExtension}.exe",
        "&&",
        "${fileDirname}/${fileBasenameNoExtension}.exe",
        "&&",
        "del ${fileDirname}/${fileBasenameNoExtension}.exe"
    ],
    "group": {
        "kind": "build",
        "isDefault": true
    }
}
